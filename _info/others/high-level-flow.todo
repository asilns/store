Store Flow (Owner/Admin/Manager/Staff/Viewer):
  signup user, user store map
  create store

  
  Login → Store Dashboard:
    KPI: заказы (сегодня/7д/30д), GMV, прибыль, средний чек.
    Блок подписки (чтение: plan, status, end date).
  
  Orders:
    Orders list (фильтр по статусу).
    Create / Edit Order (+ Order Items).
    Export CSV.
  
  Products:
    List (active/inactive).
    Create Product.
    Edit Product.
    Import CSV (с валидацией).
    Export CSV.
  
  Billing (read-only):
    Список счетов магазина.
    Скачать PDF.
  
  Settings / Users:
    Owner/Admin магазина → управляют пользователями и их ролями (manager, staff, viewer).
    Viewer = только просмотр.
    
  Страницы для Store Users:
    /dashboard
    /orders (list)
    /orders/new
    /orders/[id]/edit
    /products (list)
    /products/new
    /products/[id]/edit
    /products/import (CSV upload)
    /products/export (CSV download)
    /billing (invoices list + PDF download)
    /settings/users (user management внутри магазина)
    /settings/profile (store profile)
  
  👥 Доступ по ролям (внутри магазина):
    Owner: полный доступ (store + users).
    Store Admin: почти как Owner (без смены владельца).
    Manager: управляет заказами и продуктами.
    Staff: ограниченный CRUD (например, только заказы).
    Viewer: read-only.
  
  ==================================
  
  📌 End-to-End Сценарий (по ролям):
    Суперадмин создаёт магазин → назначает подписку.
    Owner магазина логинится → видит Dashboard.
    Owner/Admin магазина создаёт продукты → создаёт заказы.
    Owner/Admin магазина приглашает сотрудников → назначает роли.
    Manager/Staff работают с заказами и продуктами.
    Viewer только просматривает.
    Admin управляет подписками и счетами, но не лезет в пользователей.
    Cron ежедневно обновляет подписки.
    
===============================

  
🛠️ Admin Flow (Суперадмин SaaS):
Цель:
  управлять stores и их подписками.

Login → Admin Dashboard:
  KPI SaaS: MRR, #stores, распределение по планам.

Stores List:
  Название, владелец, подписка (plan, status, dates), кол-во пользователей, метрики (заказы/GMV).

Actions:
  создать магазин, приостановить/реактивировать.

Store Details:
  Информация о магазине:
  Подписка (plan, status, start/end, grace).
  Метрики: #products, #orders(30d), GMV, profit, commission totals.
  #users (только счётчик, без управления).
  Actions: редактировать/назначить подписку.
  
Subscriptions (per store):
  Создать / изменить план.
  Установить даты (start/end/grace).
  Немедленное обновление статуса.

Invoices (per store):
  Список счетов (номер, план, сумма, статус).
  HTML → PDF (скачать).

Страницы для Admin:
  /admin/dashboard
  /admin/stores (list)
  /admin/stores/[id] (store details + подписка + метрики)
  /admin/stores/[id]/subscription
  /admin/stores/[id]/invoices

